# Optional: GitHub personal access token (classic: enable "public_repo" read).
# Removes unauthenticated rate limits (60/hr per IP) → 5,000/hr.
# Copy to .env.local (never commit .env.local).
REACT_APP_GITHUB_TOKEN=